# Contributing

## Development
- Use stable Rust
- Run: `cargo fmt --all`, `cargo clippy --all-targets --all-features -D warnings`, `cargo test --all`

## Pull requests
- Keep PRs small
- Add tests for new behavior
- Update docs when changing schemas, policies, or invariants
